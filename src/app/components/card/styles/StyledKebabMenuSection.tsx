import styled from '@emotion/styled';
import { rem } from 'utilities';
import { StyledSection } from './StyledSection';

export const StyledKebabMenuSection = styled(StyledSection)({
  padding: rem(20, 4),
});
