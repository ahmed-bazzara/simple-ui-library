import styled from '@emotion/styled';
import { rem } from 'utilities';

export const StyledKebabMenuButton = styled.button({
  outline: 'none',
  appearance: 'none',
  border: 'none',
  backgroundColor: 'transparent',
  display: 'flex',
  alignItems: 'center',
  justifyItems: 'center',
  padding: rem(4),
  margin: 0,
  cursor: 'pointer',
  color: 'inherit',
});
