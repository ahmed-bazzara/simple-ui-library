import styled from '@emotion/styled';
import { rem } from 'utilities';

export const StyledContextMenuOptionIconWrapper = styled.div({
  width: rem(24),
  height: rem(24),
  marginRight: rem(16),
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
});
